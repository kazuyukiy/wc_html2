use std::net::TcpStream;

pub fn response(stream: &mut TcpStream) -> Vec<u8> {
    //     // request from stream
    //     // let mut handler = Handler::new(request);
    //     // handler.response()

    vec![]
}
